name: Pipeline

on:
  pull_request:
    types: [closed]
    paths:
      - "datasets/**"
      - "scripts/**"
      - ".github/workflows/pipeline.yml"
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:

concurrency:
  group: opi-pipeline-${{ github.event.pull_request.number || github.ref || 'schedule' }}
  cancel-in-progress: false

jobs:
  pipeline:
    if: github.event.pull_request.merged == true || github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha || github.ref }}
          fetch-depth: 0

      - uses: dataciviclab/.github/actions/python-setup@main
        with:
          extra-packages: -e ".[pipeline]"

      - name: Run datasets (toolkit)
        env:
          TOOLKIT_ALLOW_SCRIPT_SOURCE: "1"
        run: make run-all

      - name: Metriche da GCS
        run: make metrics

      - name: Cup_fatti + aggregati
        run: make layers

      - name: Panorama
        run: make panorama

      - name: Test
        run: make test

      - name: Auth GCP
        id: gcs
        uses: dataciviclab/.github/actions/gcs-auth@main
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY_OPI }}

      - name: Sync to GCS
        if: steps.gcs.outputs.ready == 'true'
        run: |
          for slug_dir in out/data/clean/*/; do
            [ -d "$slug_dir" ] || continue
            slug=$(basename "$slug_dir")
            year=$(ls "$slug_dir" 2>/dev/null | sort -rn | head -1)
            [ -z "$year" ] && echo "No clean output for $slug" && continue
            gsutil -m rsync -r "${slug_dir}${year}/" "gs://dataciviclab-clean/opere_pubbliche_intelligence/$slug/$year/"
            echo "$slug clean synced"
          done
          for slug_dir in out/data/mart/*/; do
            [ -d "$slug_dir" ] || continue
            slug=$(basename "$slug_dir")
            year=$(ls "$slug_dir" 2>/dev/null | sort -rn | head -1)
            [ -z "$year" ] && echo "No mart output for $slug" && continue
            gsutil -m rsync -r "${slug_dir}${year}/" "gs://dataciviclab-mart/opere_pubbliche_intelligence/$slug/$year/"
            echo "$slug mart synced"
          done

      - name: Registry update PR
        uses: dataciviclab/.github/actions/registry-update-pr@main
        with:
          prefix: "opere_pubbliche_intelligence"
          flat: "false"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit data/
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f data/
          if git diff --cached --quiet; then
            echo "No changes in data/ to commit"
          else
            git commit -m "ci: update data/ [skip ci]"
            git push
          fi
