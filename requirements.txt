# Dipendenze runtime del repo (dataset puro — requirements.txt e' l'unica
# fonte, ADR-001 §8). lab-connectors arriva transitivamente da toolkit.
dataciviclab-toolkit @ git+https://github.com/dataciviclab/toolkit.git@v1.51.0
duckdb>=1.5.3
openpyxl>=3.1
pytest>=8.0
